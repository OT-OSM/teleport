---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  changed_when: false

- name: Restart Teleport
  ansible.builtin.systemd:
    name: teleport
    state: restarted
    enabled: true
